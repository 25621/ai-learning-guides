# Contributor notes

## Heading anchors

Keep pin IDs (`{#kebab-id}`) equal to the slug `github-slugger` would derive
from the English heading text (Docusaurus' default slugifier). That way the pin
only locks behavior against future heading rewording, without changing the
public URL.
